<% include ../partials/_header %>

<div class="row">
    <div class="col-md-4 offset-md-4">
        <div class="card">
            <div class="card-body">
                <form action="/task/editar/<%= editar._id %>" method="post">
                    <div class="form-group">
                        <input type="text" class="form-control" name="titulo" value="<%= editar.titulo%>" autofocus>
                    </div>
                    <div class="form-group">
                        <textarea name="descripcion" class="form-control" placeholder="Deja una descripción"><%= editar.descripcion %></textarea>
                    </div>
                    <button class="btn btn-primary btn-block" type="submit">
                        Guardar Cambios
                    </button>
                </form>
            </div>
        </div>
    </div>